import type { Meta, StoryObj } from '@storybook/react';

import Bar from './Bar';

export default {
  title: 'Wpai/Charts/Bar',
  component: Bar,
};

export const Simple = {
  args: {},
};
